import React from "react";
import axios from 'axios';

const config = require('../config.json');

export default function EduGrowthToStatePage() {
    return (
        <div className="body">
            Implements Query 3, Query 8, and Query 4
        </div>
    );
}