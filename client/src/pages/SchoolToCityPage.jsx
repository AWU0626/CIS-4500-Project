import React from "react";
import axios from 'axios';


const config = require('../config.json');

export default function SchoolToCityPage() {
    return (
        <div className="body">
            Implements Query 5, Query 2
        </div>
    );
}